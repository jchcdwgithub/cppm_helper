Value Required INTERFACE (Ethernet\d/\d{1,2})
Value Required CHANNEL_GROUP (\d{1,4})
Value LAG (active|passive|desirable|auto|on)

Start
 ^ *interface ${INTERFACE}
 ^ *channel-group ${CHANNEL_GROUP} mode ${LAG} -> Record