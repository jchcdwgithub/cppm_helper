Value GROUP (\d{1,4})
Value PORT_CHANNEL (Po\d{1,3}\(.+\))
Value PROTOCOL (LACP|PagP|On)
Value PORTS (([a-zA-Z]{2}\d/\d/\d{1,2}\(.+\) +){1,8})

Start
 ^ *${GROUP} +${PORT_CHANNEL} +${PROTOCOL} +${PORTS} -> Record