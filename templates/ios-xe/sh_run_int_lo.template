Value Required INTERFACE (Loopback\d{1,4})
Value DESCRIPTION (.*)
Value IP_ADDRESS (\d{1,3}(\.\d{1,3}){3})
Value SUBNET (\d{1,3}(\.\d{1,3}){3})

Start
 ^ *interface ${INTERFACE}
 ^ *description ${DESCRIPTION}
 ^ *ip address ${IP_ADDRESS} ${SUBNET} -> Record